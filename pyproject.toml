[project]
name = "accountability-tracker"
version = "0.9.0"
description = "A lightweight macOS self-accountability and overtime tracker."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1.2",
]

[tool.uv]
package = false
